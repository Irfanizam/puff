/**
 * ESLint config for @puff/logger.
 *
 * Extends the shared base. No additional rules needed —
 * the logger package is pure TypeScript with no framework opinions.
 */

import baseConfig from "@puff/config/eslint.base.mjs";

export default [...baseConfig];
